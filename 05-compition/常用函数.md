常用函数
- 基本思路：按照题目要求，按不同周期对各外设进行任务调度
- 采用有限状态自动机的思路
- [ ] LED灯
	- HAL_GPIO_WritePin(GPIOC,GPIO_PIN_8,GPIO_PIN_RESET);//写入电平
		- ![[Pasted image 20260112154039.png]]
		- HAL_GPIO_WritePin函数的内部实现（太抽象）;
			- GPIO port bit reset register(GPIOx_BSRR) x=A TO G 
				- 
				-  0-15BS(bit set)设置为1
				- 16-31BR(bit reset)设置为0
			- 输出寄存器（GPIOx_ODR)
				- 高16位默认为零
				- pc8-pc16 0X0000FE00
- [ ] void keyscan() 键盘扫描函数，定时扫描每个键是否被按下，并返回
- [x] lcd检测  [completion:: 2026-01-12]
- [ ] Time_Task() 通过SysTick定时器实现10ms定时
- [ ] Task_Key() 不同按键按下后需要执行的任务
- [ ] 