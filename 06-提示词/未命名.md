你现在是一位资深的数据科学家和科研绘图专家。请帮我使用 Python（matplotlib + seaborn + scipy）绘制两幅【热力图 + 等值线叠加图】。

【数据背景】
我需要展示“30分钟内药材的温度”和“水分浓度”随时间和距离的变化。

· X轴变量：到药材中心的距离（离散点：0, 0.5, 1, 1.5, 2 cm）
· Y轴变量：时间（离散点：100, 300, 600, 900, 1200, 1500, 1800 s）
· 指标A：温度（Table 1）
· 指标B：水分浓度（Table 2）

【当前数据状态】
我在文件夹下放置对应的csv文件
table1->

【绘图具体要求】

1. 画布布局：生成一个 1行2列 的并列子图（图A为温度，图B为水分浓度）。
2. 热力图：使用 imshow 或 pcolormesh 绘制热力图。温度图使用 inferno 或 magma 配色（黑-红-黄），水分图使用 viridis 或 Blues 配色（蓝-绿-黄）。
3. 等值线叠加：使用 scipy.interpolate 对离散数据进行平滑插值（推荐 cubic 或 linear），并在热力图上方叠加黑色细等值线（contour）。等值线上必须使用 clabel 标注具体数值（保留1位小数）。
4. 坐标轴设置：X轴刻度为 [0, 0.5, 1, 1.5, 2]；Y轴刻度为 [100, 300, 600, 900, 1200, 1500, 1800]（注意：为了让时间更符合物理直觉，Y轴建议自上而下递增，或者由下往上递增，请选择最符合传质传热逻辑的方式）。
5. 科研细节：
   · 添加带有标签的 Colorbar（如 Temperature (°C) 和 Moisture Concentration (%)）。
   · 去除多余的边框，刻度线向内（top和right也带刻度）。
   · 字体大小调整为适合学术论文（如字号12-14）。
   · 使用色盲友好配色。
6. 输出要求：请给出完整的、可直接运行的 Python 代码，并在代码中加好注释。如果我要替换成真实数据，只需修改哪一部分代码，请给予说明。
