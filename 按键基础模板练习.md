---
created: 2026-04-05
tags:
  - 蓝桥杯
  - finished
---

```C
typedef struct{
	uint8_t judge_state;
	bool short_flag;
	bool long_flag;
	uint32_t press_tick;
}Key_struct;


Key_struct keys[4];

void key_scan()
{
	short_time_judge;
	uint8_t key_value[4];
	key_value[1] = HAL_GPIO_ReadPin(GPIOB,GPIO_PIN_0);
	key_value[2] = HAL_GPIO_ReadPin(GPIOB,GPIO_PIN_1);
	key_value[3] = HAL_GPIO_ReadPin(GPIOB,GPIO_PIN_2);
	key_value[4] = HAL_GPIO_ReadPin(GPIOA,GPIO_PIN_0);

	for(int i = 0; i<4;i++)
	{
		switch(keys[i].judges_state)
		{case 0://判断是否按下
			if(key_value[i] == 0)
			{
				keys[i].judge_state = 1;//进入下一消抖阶段
				keys[i].press_tick = HAL_GetTick();
			}
			break;
		case 1:
			if(key_value[i] == 1)
	
				keys[i].judge_state = 0;//发生抖动
			}else if(HAL_GetTick() - keys[i].press_tick>=10)//大于等于消抖的时长才能进入下一个阶段
			{
			keys[i].judge_state = 2;
			}
		break;
		case 2://等待松手
			if(HAL_GetTick()- keys[i].press_tick < 800)
			{
			keys[i].short_flag = 1;
			keys[i].judge_state = 0;
			}
			else if(HAL_GetTick()- keys[i].press_tick >= 800)
			{
			keys[i].long_flag = true;
			keys[i].judge_state=3;//进入等待长按结束的死区
			}
			break;
			   case 3: // 状态 3：长按后的死区，等待按键抬起
                if (key_val[i] == 1) Keys[i].judge_sta = 0;
                break;
		
		}
	}
}
```
如何使用这个函数？
因为定义的按键状态为全局变量，所以可以单独写在不同状态下的函数，从而将按键读取和状态导致的行为分离开