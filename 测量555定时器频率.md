# 环境配置
- 根据原理图找到PA15，PB4，
- 配置定时器，设置输入捕获，psc80－1
- 代码添加的注意事项，一定要添加路径！！！，编译一下
# 代码设置
```C
//主函数中,使能中断,这个函数会同时使能定时器和中断
HAL_TIM_IC_Start_IT(&htim2,TIM_CHANNEL_1);
HAL_TIM_IC_Start_IT(&htim16,TIM_CHANNEL_1);
//




//fun.c中
uint32_t fre1 ,capture_value1,fre2,capture_value2;


void C
{
	if(htim->Instance == TIM2)
	{
	capture_value1 = TIM2->CCR1;
		//capture_value = HAL_TIM_ReadCaptureValue(htim,TIM_CHANNEL_1);//在tim.h中找到ctrl f找到readcapture 函数
		TIM17->CNT = 0;
		fre1 = (80000000/(80*capture_value));
	}
	else if(htim->Instance == TIM16)

}
```